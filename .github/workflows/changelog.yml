name: Changelog

on:
  push:
    branches:
      - main

jobs:
  update:
    name: Prepare
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@v7.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
